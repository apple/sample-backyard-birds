/*
See the LICENSE.txt file for this sample’s licensing information.

Abstract:
A convenience extension to `ImageResource`.
*/

import SwiftUI

public extension ImageResource {
    static let fountain = ImageResource(name: "fountain", bundle: .module)
    static let fountainFill = ImageResource(name: "fountain.fill", bundle: .module)
}
